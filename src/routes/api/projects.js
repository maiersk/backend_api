import Router from 'koa-router'

const projects = new Router()

projects.get('/', (ctx, next) => {
  
})

projects.get('/:id', (ctx, next) => {
  
})

projects.post('/', (ctx, next) => {
  
})

projects.put('/:id', (ctx, next) => {
  
})

projects.delete('/:id', (ctx, next) => {
  
})

export default projects
